import { IModel } from "../../models/commonModel";

const homeDashboardNavigations: IModel[] = [
  {
    label: "Home",
    items: [{ label: "Dashboard", icon: "pi pi-fw pi-home", to: "/" }],
  },
];

export default homeDashboardNavigations;
