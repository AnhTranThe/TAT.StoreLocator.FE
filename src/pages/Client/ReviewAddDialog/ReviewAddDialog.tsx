import { IReviewModel } from "../../../models/reviewModel";

export default function ReviewAddDialog({ dialogVisible, onHide, detailReview }: { dialogVisible: boolean, onHide: () => void, detailTask: IReviewModel }) {
    return (
        <div>ReviewAddDialog</div>
    )
}
